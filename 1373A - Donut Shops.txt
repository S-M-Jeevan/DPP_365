#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define Fast_IO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
//functions
#define yes puts("YES")
#define no puts("NO")

int main() {
    Fast_IO
    ll t;
    cin>>t;
    while(t--){
        ll a,b,c; cin>>a>>b>>c;
        ll s1=-1,s2=-1;
        if (a<c) s1=1;
        if (c<a*b) s2=b;
        cout<<s1<<" "<<s2<<endl;
    }
}