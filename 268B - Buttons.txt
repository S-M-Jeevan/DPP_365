#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define Fast_IO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
//functions
#define yes puts("YES")
#define no puts("NO")

int main() {
    Fast_IO
    ll n; cin>>n;
    ll sum=0;
    for(ll i=1;i<=n;i++){
        sum+=i*(n-i);
    }
    sum+=n;
    cout<<sum<<endl;
}